# docker-registry-ui
docker registry and ui

```
.
├── README.md
├── docker-compose.yml
├── registry-config
│   └── config.yml
├── registry-data
└── test.sh
```

step 1:

```sh
docker-compose up -d
```

step 2:

http://localhost
